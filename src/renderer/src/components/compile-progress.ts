/**
 * compile-progress — railroad-themed indeterminate progress indicator
 * displayed while compilation / upload is running.
 *
 * All animation is handled by CSS; no JavaScript runtime needed.
 */
export class CompileProgressCustomElement { }
